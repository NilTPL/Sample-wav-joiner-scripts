# Sample-wav-joiner


requirements:

 - python,
 - ffmpeg,
 - python-ffmpeg (`pip install python-ffmpeg`)


first, run `correct-naming-convention.py` in the directory of choice (preferably topmost folder of your extracted sample CD).
next, run `batch-join-script` to merge samples together.
